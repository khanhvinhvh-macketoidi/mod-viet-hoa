export default function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { return <AuthForm searchParams={searchParams}/> }
async function AuthForm({searchParams}:{searchParams:Promise<{error?:string}>}) { const p=await searchParams; return <section className="mx-auto max-w-md px-5 py-16"><h1 className="text-3xl font-black">Đăng nhập</h1>{p.error && <p className="mt-4 rounded-lg bg-red-950 p-3 text-red-200">Email hoặc mật khẩu không đúng.</p>}<form action="/api/auth/login" method="post" className="mt-7 space-y-4"><input type="email" name="email" placeholder="Email" required/><input type="password" name="password" placeholder="Mật khẩu" required/><button className="w-full rounded-xl bg-amber-400 p-3 font-bold text-slate-950">Đăng nhập</button></form></section> }
